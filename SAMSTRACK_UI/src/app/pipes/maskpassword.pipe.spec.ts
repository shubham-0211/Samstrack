import { MaskpasswordPipe } from './maskpassword.pipe';

describe('MaskpasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskpasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
